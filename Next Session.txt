SESSION HANDOFF — 2026-05-07
=============================

WHAT WAS DONE THIS SESSION
-----------------------------
1. Deployed to GitHub Pages — live at https://geomeskin.github.io/FoE_CityPlanner/
2. Fixed null element crash on city import (missing id="fixed-summary")
3. Replaced mock packer with Simulated Annealing optimizer:
   - 6 independent runs x 5000 iterations each
   - Each run starts from a random shuffle of movable buildings
   - Score = Manhattan distance to town hall (road buildings 2x weight)
   - Move = swap two random movable buildings if both positions valid
   - Keeps best result across all 6 runs
4. Off-site buildings (Friends Tavern, outposts) excluded from packing
5. Added "Back to City" button on redesign results screen
6. Roads changed to black (#111111)
7. Building borders made visible (dark 1px stroke) on both canvases
8. Stats bar added below legend (Buildings, GBs, Unlocked Cells, Need Road, Road-Free, Road Segments)
9. Per-cell borders on unlocked area cells for easy space counting
10. Filename/Player ID displayed in toolbar after city load
11. Save/Load city versions via IndexedDB (survives page reloads)
12. Export saved city as JSON file (for cross-device transfer)
13. Import saved city JSON file back into IndexedDB
14. Elapsed timer on "Plan Redesign" button (shows 0:00 counting up)

WHAT STILL NEEDS DOING (in order)
------------------------------------
1. ROAD GENERATION — still the biggest gap in the redesign output.
   The SA optimizer scores by distance to town hall but draws NO roads.
   Algorithm to build:
     a) Find town hall (type === 'main_building')
     b) Start with its perimeter cells as the "road frontier"
     c) For each road-requiring building (b.road === true), BFS from building
        outward until hitting the frontier, then add those path cells as roads
     d) Newly added road cells join the frontier for subsequent buildings
     e) Return array of {x, y, w:1, l:1} road tiles
   Wire result into redrawRedesignCanvas (draw roads before buildings, black)

2. SA TUNING — need to observe actual run times and result quality.
   Currently: 6 runs x 5000 iterations. Previous session had ~10 min runs.
   May need to increase iterations or adjust T0/T_MIN/cooling schedule.
   Road tile count should replace/augment distance score once road gen is built.

3. SPACE SLIDER — sl-space value is still unused in runSA.
   Idea: weight compactness score by slider value.

4. SET DETECTION — colored dots on buildings belonging to the same named set.

KEY FILE / LINE REFERENCES
-----------------------------
- index.html ~1476  onRedesignButtonClick() — entry point, has elapsed timer
- index.html ~1495  runSA()                 — SA optimizer (replace/extend here)
- index.html ~1580  redrawRedesignCanvas()  — add road drawing here (before buildings)
- redesign-output.js initializeMetricsPanel() — metrics side panel

DATA SHAPES
------------
- cityState.buildings    [{type, name, x, y, w, l, road}]  road=true → needs adjacency
- cityState.unlockedAreas [{x, y, width, length}]           some omit y → treat as 0
- cityState.fixedSet     Set of "x,y" strings
- cityState.roads        [{x, y, w, l}]                     original road tiles
- Town hall:             b.type === 'main_building'

GITHUB
-------
- Repo:  https://github.com/geomeskin/FoE_CityPlanner
- Live:  https://geomeskin.github.io/FoE_CityPlanner/
- Branch: main (GitHub Pages serves from main / root)
